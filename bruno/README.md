# Sabeel API Bruno Collection

Open this `bruno/` directory in Bruno.

Use the `local` environment for local development. `Auth/Register` and `Auth/Login` open the Zitadel browser flow. Keep Bruno's cookie jar enabled so protected requests use the resulting opaque `sabeel_session` cookie.
